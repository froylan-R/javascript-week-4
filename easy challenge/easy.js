const average = function (arr) {
    let number = 0;
    for(let i = 0; i < arr.length; i++) {
        return number = number + arr[i];
    }
    return number / arr.length;
}


console.log(average([9, 5, 7]));